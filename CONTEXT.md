# SOOP 댓글 알림 필터 — 작업 컨텍스트

이 문서는 이후 유지보수 작업에서 필요한 배경, 실제 SOOP DOM 구조, 확정된 동작 정책과 주의사항을 지속해서 기록하는 문서입니다. 기능 변경이나 새로운 SOOP DOM 정보를 확인할 때 함께 갱신합니다.

## 프로젝트 목표

SOOP 웹사이트의 알림 목록에서 댓글과 답글 알림을 쉽게 모아보기 위한 Manifest V3 Chrome 확장 프로그램입니다. SOOP 서버 요청이나 계정 데이터에는 개입하지 않고, 현재 페이지에 렌더링된 알림을 분류하여 CSS로만 숨깁니다.

## 확정된 사용자 요구사항

- SOOP 알림창 안에서 `댓글만 / 전체` 필터를 제공합니다.
- 알림창을 열었을 때 기본 모드는 `댓글만`입니다.
- 댓글과 답글을 댓글 알림으로 처리합니다.
- 명백한 비댓글 알림은 `댓글만` 모드에서 숨깁니다.
- 종류를 확실히 판별하지 못한 알림은 누락 방지를 위해 표시합니다.
- SOOP 종 아이콘의 읽지 않은 알림 숫자는 변경하지 않습니다.
- 날짜마다 필터를 반복하지 않고 모든 날짜를 제어하는 필터 하나만 표시합니다.
- 필터 결과가 없는 날짜는 날짜 제목과 빈 목록을 함께 숨깁니다.
- `전체`를 선택하면 모든 알림과 날짜를 원래 순서대로 복원합니다.
- 별도 진단 UI, 저장소 권한, 원격 서버 또는 SOOP 비공개 API를 사용하지 않습니다.

## 확인된 SOOP DOM 구조

사용자가 2026-07-21에 제공한 실제 알림 DOM을 기준으로 합니다.

```html
<ul class="notification-module_list__IkcCv">
  <li>
    <div class="__soopui__Notification-module__listItem___GJktv">
      ...
      <p class="__soopui__Notification-module__title___bRezj">
        알림 제목
      </p>
      ...
    </div>
  </li>
</ul>
```

- 날짜별로 `ul.notification-module_list__…`가 하나씩 생성됩니다.
- 각 `<ul>`의 직계 `<li>` 하나가 알림 하나입니다.
- 날짜 제목은 목록 앞에 있으며 `오늘`, `어제`, `N월 N일` 형태입니다.
- CSS 모듈 해시인 `IkcCv`, `GJktv`, `bRezj` 등은 변경될 수 있으므로 고정값으로 사용하지 않습니다.
- 목록 선택자는 `ul[class*='notification-module_list__' i]`를 사용합니다.
- 실제 날짜별 알림 목록이 없으면 필터도 삽입하지 않습니다. 범용 `notification` 클래스 탐색은 설정·정렬·빈 상태 영역을 패널로 오인한 실제 사례가 있어 제거했습니다.

## 분류 정책

분류기는 `src/classifier.js`, DOM 처리는 `src/content.js`에 분리되어 있습니다.

- `comment`: `댓글`, `답글`, 영문 comment/reply 문구가 명시된 알림
- `non-comment`: 방송 시작·종료, 라이브, 공지, 신규 게시글, 게시글 UP, 구독, 이벤트, 선물, 별풍선, 팔로우, 즐겨찾기, VOD, 클립 등
- `unknown`: 위 규칙으로 확정할 수 없는 알림

댓글 관련 표현이 다른 키워드와 함께 있으면 댓글 판정을 우선합니다. 예를 들어 `공지 게시물에 댓글이 달렸습니다`와 `VOD에 댓글을 남겼습니다`는 댓글 알림입니다.

단, `님의 신규 게시글:`과 `님이 게시글을 UP 했어요`처럼 SOOP이 제공하는 고정 알림 유형은 사용자 작성 제목보다 먼저 판정합니다. 게시글 제목에 `댓글`이나 `답글`이 포함돼도 해당 알림은 `non-comment`입니다.

`댓글만` 모드에서는 `non-comment`만 숨기며 `comment`와 `unknown`은 표시합니다.

## DOM 및 Observer 설계

- SOOP가 React로 알림창과 목록을 동적으로 다시 렌더링하므로 `MutationObserver`를 사용합니다.
- 전역 observer는 `childList`만 감시합니다. 페이지 전체의 `class`, `style`, `hidden`, `aria-hidden` 속성 변경은 감시하지 않습니다.
- 변경된 대상이 기존 알림 목록 내부이거나 추가·삭제된 하위 트리에 실제 알림 목록이 포함된 경우에만 재스캔합니다.
- 방송 카드, 배너, 슬라이드처럼 알림과 관계없는 SOOP DOM 변경은 무시합니다.
- 첫 번째 날짜 목록 앞에 전역 필터 UI 하나를 삽입합니다.
- 필터 UI는 목록 `<ul>` 내부에 넣지 않습니다. SOOP의 `<li>` 스타일 상속과 날짜별 중복을 방지하기 위해서입니다.
- 모든 날짜 목록의 직계 `<li>`를 한 번에 필터링합니다.
- 숨김은 `.soop-comment-filter-hidden`, 빈 날짜 숨김은 `.soop-comment-filter-empty-date`를 사용합니다.
- 확장이 삽입한 `.soop-comment-filter`와 자체 클래스·속성 변경은 observer 재스캔 대상에서 제외합니다.
- SOOP이 새 알림 `<li>`를 추가하거나 목록을 교체하는 변경은 계속 감지합니다.
- 알림이 0개인 상태에서는 SOOP 설정 영역, 정렬 드롭다운, 빈 상태 UI에 필터가 생성되지 않아야 합니다.
- 페이지가 종료될 때 확장이 삽입한 UI, 클래스 및 데이터 속성을 정리합니다.

## UI 정책

- 버튼 문구는 `댓글만`, `전체`입니다.
- 활성 버튼은 파란 배경과 흰색 글자를 강제 적용합니다. SOOP의 전역 버튼 CSS가 확장 스타일을 덮어쓴 사례가 있어 필요한 처리입니다.
- SOOP 화면 우측 상단의 기본 `전체` 메뉴는 이 확장 필터와 관련이 없으며 수정하지 않습니다.
- 확장 아이콘은 SOOP 공식 페이지가 지정한 `https://res.sooplive.com/favicon.ico`를 원본으로 사용합니다.

## 저장소 구조

현재 배포 버전은 `1.0.5`입니다.

- `manifest.json`: 최신 확장 매니페스트
- `src/`: 최신 분류·DOM·스타일 소스
- `icons/`: 공식 SOOP 파비콘 원본 및 Chrome 아이콘 크기별 PNG
- `test/`: Node 내장 테스트 러너 기반 테스트
- `README.md`: 사용자용 설치, 사용, 배포, 변경 이력
- `CONTEXT.md`: 유지보수용 배경과 결정 기록
- `soop-comment-filter.zip`: 최신 배포본
- `archive/soop-comment-filter-v1.0.0/`: 이전 버전 보관본

Chrome에는 저장소 루트의 `manifest.json`이 있는 폴더를 불러옵니다. `archive/`는 실행 대상이 아닙니다.

## 검증 및 배포

자동 테스트:

```sh
npm test
```

배포 ZIP:

```sh
zip -r soop-comment-filter.zip manifest.json src icons README.md CONTEXT.md
```

실사이트 수동 확인 항목:

1. 필터가 알림 목록에 한 번만 표시되는지
2. `댓글만` 글씨와 활성 상태가 명확히 보이는지
3. 댓글·답글 및 unknown 알림이 표시되는지
4. 방송, 신규 게시글, VOD 등 비댓글 알림이 숨겨지는지
5. 결과가 없는 날짜 제목도 숨겨지는지
6. `전체`에서 모든 알림과 날짜가 복원되는지
7. 추가 로딩과 React 재렌더링 후에도 필터가 중복되지 않는지
8. 확장 자체 DOM 변경으로 반복 스캔하지 않는지

## 유지보수 기록 원칙

- 새로운 SOOP DOM 샘플을 받으면 이 문서의 DOM 구조와 날짜·알림 관계를 갱신합니다.
- 사용자와 합의한 표시 정책이 바뀌면 `확정된 사용자 요구사항`과 `분류 정책`을 함께 갱신합니다.
- 구현상 중요한 우회 또는 CSS 충돌 해결은 `DOM 및 Observer 설계`나 `UI 정책`에 이유와 함께 기록합니다.
- 사용자용 변경 사항은 `README.md` 변경 이력에도 기록합니다.
- 버전을 올리지 않은 변경도 해당 버전 항목 아래에 명시합니다.
